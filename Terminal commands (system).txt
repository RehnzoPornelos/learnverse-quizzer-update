MAIN PC COMMANDS

(ADDRESS FOR MAIN PC - add on .env.local)
VITE_BACKEND_URL=http://172.16.20.32:8000
VITE_PASSWORD_URL=http://172.16.20.32:8080

cd backend
uvicorn main:socket_app --host 0.0.0.0 --port 8000 --reload

(New terminal)
npm run dev

(For supabase edits)
npm i -D supabase
npx supabase login
npx supabase gen types typescript --project-id tjmtqmwhkqrbscxsegjl --schema public > src/integrations/supabase/types.ts

(For Github commits)
git add -A
git commit -m "Your commit title"
git push

(if no upstream set)
git push -u origin main

STEP-BY-STEP DEPLOYMENT

(for bypassing)
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy Unrestricted

(check versions)
node -v   # ideal: v22.13.1
npm -v    # ideal: 10.9.2
python -V # ideal: 3.12.1

(packages for backend)
pip install -r backend/requirements.txt

(packages for frontend)
npm ci --no-audit --no-fund